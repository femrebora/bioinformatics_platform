docker.enabled     = true
docker.runOptions  = '-u $(id -u):$(id -g)'

process {
    executor      = 'local'
    errorStrategy = { task.exitStatus in [143, 137] ? 'retry' : 'finish' }
    maxRetries    = 1
}
